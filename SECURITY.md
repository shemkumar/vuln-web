# Security Notice

This repository is intentionally insecure. It is a scanner test fixture, not a deployable app. All credentials are fake test strings, and every vulnerable pattern should be treated as deliberate.
